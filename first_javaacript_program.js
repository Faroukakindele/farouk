//solving problems using the quadratic equation
let a=1
let b=5
let c=6
let denominator = 2*a
let positive_numerator = (-b+Math.sqrt(Math.pow(b,2)-4*a*c))
let negative_numerator = (-b-Math.sqrt(Math.pow(b,2)-4*a*c))
let x_positive = positive_numerator/denominator
let x_negative = negative_numerator/denominator
